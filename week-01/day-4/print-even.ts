'use strict';

// Create a program that prints all the even numbers between 0 and 500

for (let iPromise: number = 0; iPromise < 501; iPromise++) {
  if (iPromise % 2 == 0) {
    console.log(iPromise);
  }
}
