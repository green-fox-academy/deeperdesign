'use strict';

// Create a program that writes this line 100 times:
// "I won't cheat on the exam!"

for (let iPromise: number = 0; iPromise < 99; iPromise++) {
  console.log(`I won't cheat on the exam!`);
}
